# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-server
deploy-stage
*.zip
.azure/
*.local

# Environment variables (contains API keys)
.env
.env.local
.env.*.local

# One-off secret handoff file — delete once pasted into GitHub
azure-secrets.local.txt

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Uncompressed source for server/building-cache.json.gz (regenerate with gzip -9)
server/building-cache.json

# Playwright MCP session artifacts (snapshots and screenshots land in the
# repo root). Deliberately not a blanket *.png rule, which would silently
# swallow legitimate assets under src/assets and public/.
.playwright-mcp/
/*.png
